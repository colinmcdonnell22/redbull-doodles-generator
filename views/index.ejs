<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Higher - AI Image Generation</title>
    <style>
        /* General Styles */
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(-45deg, #f0f7f0, #e0f2e0, #d0edd0, #f5f9f5);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            color: #1a1a1a;
            margin: 0;
            padding: 2rem;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .container {
            background: #ffffff;
            border: 1px solid #e0e0e0;
            padding: 2rem;
            border-radius: 12px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2rem;
            margin: 0 0 1.5rem 0;
            color: #15803d;
            font-weight: 700;
            text-align: center;
        }

        .subtitle {
            text-align: center;
            color: #4b5563;
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        label {
            font-weight: 600;
            font-size: 0.9rem;
            color: #374151;
            display: block;
            margin-bottom: 0.5rem;
        }

        textarea {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            background: #f9fafb;
            font-size: 1rem;
            color: #1f2937;
            min-height: 80px;
            resize: vertical;
            font-family: inherit;
            box-sizing: border-box;
        }

        textarea:focus {
            outline: none;
            border-color: #15803d;
            box-shadow: 0 0 0 3px rgba(21, 128, 61, 0.1);
        }

        .settings-toggle {
            color: #15803d;
            background: none;
            border: none;
            padding: 0.5rem;
            font-size: 0.9rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0 auto;
        }

        .settings {
            display: none;
            background: #f0f7f0;
            padding: 1.5rem;
            border-radius: 8px;
            margin-top: 1rem;
        }

        .slider-container {
            margin-bottom: 1.5rem;
        }

        .slider-container:last-child {
            margin-bottom: 0;
        }

        .slider-label {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .slider-value {
            color: #15803d;
            font-weight: 600;
        }

        input[type="range"] {
            width: 100%;
            accent-color: #15803d;
        }

        button[type="submit"] {
            background-color: #15803d;
            color: white;
            border: none;
            padding: 1rem;
            border-radius: 6px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s, transform 0.2s;
        }

        button[type="submit"]:hover {
            background-color: #166534;
            transform: translateY(-1px);
        }

        button[type="submit"]:active {
            transform: translateY(1px);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Higher</h1>
        <p class="subtitle">Transform your ideas into stunning images</p>
        
        <form action="/generate" method="POST">
            <div>
                <label for="prompt">What would you like to create?</label>
                <textarea 
                    id="prompt" 
                    name="prompt" 
                    placeholder="Describe your vision..."
                    required></textarea>
            </div>

            <button type="button" class="settings-toggle" onclick="toggleSettings()">
                Advanced Settings ↓
            </button>

            <div id="settings-container" class="settings">
                <div class="slider-container">
                    <div class="slider-label">
                        <label for="lora_scale">Creative Freedom</label>
                        <span class="slider-value" id="lora_value">1.0</span>
                    </div>
                    <input type="range" id="lora_scale" name="lora_scale" 
                           min="0" max="3" step="0.1" value="1" 
                           oninput="updateValue('lora_value', this.value)">
                </div>

                <div class="slider-container">
                    <div class="slider-label">
                        <label for="guidance_scale">Style Adherence</label>
                        <span class="slider-value" id="guidance_value">3.0</span>
                    </div>
                    <input type="range" id="guidance_scale" name="guidance_scale" 
                           min="1.0" max="20.0" step="0.1" value="3.0" 
                           oninput="updateValue('guidance_value', this.value)">
                </div>

                <div class="slider-container">
                    <div class="slider-label">
                        <label for="prompt_strength">Impact Level</label>
                        <span class="slider-value" id="strength_value">0.8</span>
                    </div>
                    <input type="range" id="prompt_strength" name="prompt_strength" 
                           min="0.0" max="1.0" step="0.1" value="0.8" 
                           oninput="updateValue('strength_value', this.value)">
                </div>

                <div class="slider-container">
                    <div class="slider-label">
                        <label for="num_inference_steps">Quality Level</label>
                        <span class="slider-value" id="steps_value">28</span>
                    </div>
                    <input type="range" id="num_inference_steps" name="num_inference_steps" 
                           min="1" max="100" step="1" value="28" 
                           oninput="updateValue('steps_value', this.value)">
                </div>
            </div>

            <button type="submit">Generate Image</button>
        </form>
    </div>

    <script>
        function updateValue(id, value) {
            document.getElementById(id).innerText = value;
        }

        function toggleSettings() {
            const settings = document.getElementById("settings-container");
            settings.style.display = settings.style.display === "block" ? "none" : "block";
        }
    </script>
</body>
</html>