<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tweak Generator</title>
    <style>
        /* General Reset and Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(180deg, #fdfdfd, #f0f0f5);
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: #ffffff;
            border-radius: 15px;
            padding: 2rem;
            max-width: 600px;
            width: 90%;
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
            border: 1px solid #e5e5e5;
            animation: fadeIn 0.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h1 {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 1rem;
            text-align: center;
            color: #222;
        }

        p.subtitle {
            font-size: 1rem;
            color: #666;
            margin-bottom: 2rem;
            text-align: center;
        }

        form {
            display: grid;
            gap: 1.5rem;
        }

        label {
            font-weight: bold;
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        input[type="range"], input[type="text"] {
            width: 100%;
            margin: 0.5rem 0 1rem;
        }

        input[type="range"] {
            -webkit-appearance: none;
            appearance: none;
            height: 8px;
            background: #ddd;
            border-radius: 4px;
            outline: none;
            transition: background 0.2s ease;
        }

        input[type="range"]::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 16px;
            height: 16px;
            background: #6c63ff;
            border-radius: 50%;
            cursor: pointer;
        }

        input[type="range"]::-moz-range-thumb {
            width: 16px;
            height: 16px;
            background: #6c63ff;
            border-radius: 50%;
            cursor: pointer;
        }

        .helper-text {
            font-size: 0.85rem;
            color: #555;
            margin-bottom: 0.5rem;
        }

        button {
            background: linear-gradient(90deg, #6c63ff, #7a88ff);
            color: #fff;
            font-size: 1rem;
            font-weight: bold;
            padding: 0.8rem 1.2rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-transform: uppercase;
            box-shadow: 0 4px 12px rgba(108, 99, 255, 0.4);
            transition: background 0.3s ease, transform 0.2s ease;
        }

        button:hover {
            background: linear-gradient(90deg, #7a88ff, #6c63ff);
            transform: translateY(-2px);
        }

        button:active {
            transform: translateY(0);
        }

        .slider-value {
            font-size: 0.9rem;
            font-weight: bold;
            color: #444;
            text-align: right;
        }

        .slider-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Tweak Generator</h1>
        <p class="subtitle">Fine-tune the sliders to create your perfect tweak!</p>
        <form action="/generate" method="POST">
            <!-- Prompt -->
            <label for="prompt">What's your idea?</label>
            <input type="text" id="prompt" name="prompt" placeholder="e.g., A futuristic robot in Tweak style" required>

            <!-- Guidance Scale -->
            <label for="guidance_scale">How abstract do you want the tweak?</label>
            <p class="helper-text">Higher values make it more abstract, lower values keep it grounded.</p>
            <div class="slider-container">
                <input type="range" id="guidance_scale" name="guidance_scale" min="1" max="20" step="0.5" value="3" oninput="updateValue('guidance', this.value)">
                <span class="slider-value" id="guidance">3</span>
            </div>

            <!-- Inference Steps -->
            <label for="num_inference_steps">How detailed should the tweak be?</label>
            <p class="helper-text">Lower values make it realistic and sharp; higher values make it abstract and bubbly.</p>
            <div class="slider-container">
                <input type="range" id="num_inference_steps" name="num_inference_steps" min="1" max="50" value="28" oninput="updateValue('steps', this.value)">
                <span class="slider-value" id="steps">28</span>
            </div>

            <!-- Prompt Strength -->
            <label for="prompt_strength">How much should it match the Tweak style?</label>
            <p class="helper-text">Higher values align closely with Tweak’s iconic look, lower values make it deviate.</p>
            <div class="slider-container">
                <input type="range" id="prompt_strength" name="prompt_strength" min="0" max="1" step="0.1" value="0.8" oninput="updateValue('prompt_strength', this.value)">
                <span class="slider-value" id="prompt_strength">0.8</span>
            </div>

            <!-- Hidden Inputs -->
            <input type="hidden" id="num_outputs" name="num_outputs" value="1">

            <!-- Submit Button -->
            <button type="submit">Generate My Tweak!</button>
        </form>
    </div>

    <script>
        function updateValue(id, value) {
            document.getElementById(id).innerText = value;
        }
    </script>
</body>
</html>
