<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tweak Generator</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #fdfdfd;
            color: #333;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .generator-container {
            max-width: 600px;
            width: 100%;
            background: #ffffff;
            border: 1px solid #eaeaea;
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0px 8px 30px rgba(0, 0, 0, 0.1);
            text-align: left;
        }

        h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: #222;
            letter-spacing: -0.02em;
        }

        p {
            font-size: 1rem;
            color: #555;
            margin-bottom: 20px;
            line-height: 1.5;
        }

        form label {
            font-weight: 600;
            margin-bottom: 10px;
            display: block;
            font-size: 0.95rem;
            color: #333;
        }

        input, select {
            width: 100%;
            padding: 12px;
            font-size: 1rem;
            margin-bottom: 20px;
            border: 1px solid #eaeaea;
            border-radius: 6px;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        input:focus, select:focus {
            outline: none;
            border-color: #4f46e5;
            box-shadow: 0px 0px 4px rgba(79, 70, 229, 0.5);
        }

        .slider-container {
            margin-bottom: 20px;
        }

        .slider-label {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 0.9rem;
            color: #333;
        }

        .slider {
            -webkit-appearance: none;
            width: 100%;
            height: 8px;
            border-radius: 4px;
            background: #ddd;
            outline: none;
            transition: background-color 0.2s;
        }

        .slider:hover {
            background-color: #ccc;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: #4f46e5;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .slider::-webkit-slider-thumb:hover {
            background: #3a37c7;
        }

        .button {
            display: inline-block;
            width: 100%;
            background-color: #4f46e5;
            color: white;
            padding: 15px;
            text-align: center;
            font-size: 1rem;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            text-transform: uppercase;
            transition: background-color 0.3s, transform 0.2s;
        }

        .button:hover {
            background-color: #3a37c7;
            transform: translateY(-2px);
        }

        .button:active {
            transform: translateY(0);
        }

        @media (max-width: 768px) {
            .generator-container {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="generator-container">
        <h1>Tweak Generator</h1>
        <p>Design your unique Tweak! Use the sliders and dropdowns to customize your creation. Let your imagination run wild!</p>
        <form action="/generate" method="POST">
            <!-- Prompt Input -->
            <label for="prompt">Describe your Tweak</label>
            <input type="text" id="prompt" name="prompt" placeholder="e.g., A futuristic robot in Tweak style" required>

            <!-- LoRA Scale Slider -->
            <div class="slider-container">
                <div class="slider-label">
                    <span>How much LoRA magic?</span>
                    <span id="lora-scale-value">1</span>
                </div>
                <input type="range" id="lora_scale" name="lora_scale" min="0" max="3" step="0.1" value="1" class="slider" oninput="updateSliderValue('lora_scale', this.value)">
            </div>

            <!-- Number of Outputs -->
            <label for="num_outputs">Number of images</label>
            <input type="number" id="num_outputs" name="num_outputs" min="1" max="5" value="1">

            <!-- Aspect Ratio Dropdown -->
            <label for="aspect_ratio">Aspect ratio</label>
            <select id="aspect_ratio" name="aspect_ratio">
                <option value="1:1">1:1 (Square)</option>
                <option value="16:9">16:9 (Landscape)</option>
                <option value="9:16">9:16 (Portrait)</option>
            </select>

            <!-- Guidance Scale Slider -->
            <div class="slider-container">
                <div class="slider-label">
                    <span>How much guidance?</span>
                    <span id="guidance-scale-value">3.0</span>
                </div>
                <input type="range" id="guidance_scale" name="guidance_scale" min="1" max="20" step="0.5" value="3.0" class="slider" oninput="updateSliderValue('guidance_scale', this.value)">
            </div>

            <!-- Submit Button -->
            <button type="submit" class="button">Generate My Tweak!</button>
        </form>
    </div>

    <script>
        function updateSliderValue(id, value) {
            document.getElementById(`${id}-value`).textContent = value;
        }
    </script>
</body>
</html>
