<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tweak Generator</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f7f8fa;
            color: #333;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .generator-container {
            background: #fff;
            padding: 40px;
            width: 100%;
            max-width: 500px;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: #222;
        }

        p {
            font-size: 1rem;
            color: #555;
            margin-bottom: 20px;
        }

        label {
            font-weight: 600;
            margin-bottom: 8px;
            display: block;
            text-align: left;
            color: #333;
        }

        input, select {
            width: 100%;
            padding: 10px;
            font-size: 1rem;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-sizing: border-box;
        }

        input:focus, select:focus {
            outline: none;
            border-color: #6C63FF;
            box-shadow: 0px 0px 4px rgba(108, 99, 255, 0.4);
        }

        .slider {
            -webkit-appearance: none;
            width: 100%;
            height: 10px;
            border-radius: 5px;
            background: #ddd;
            outline: none;
            opacity: 0.9;
            transition: opacity 0.2s;
            margin-bottom: 20px;
        }

        .slider:hover {
            opacity: 1;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #6C63FF;
            cursor: pointer;
        }

        .button {
            display: inline-block;
            width: 100%;
            background-color: #6C63FF;
            color: white;
            padding: 15px 20px;
            text-align: center;
            font-size: 1rem;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            text-transform: uppercase;
            transition: background-color 0.3s, transform 0.2s;
        }

        .button:hover {
            background-color: #5848d4;
            transform: translateY(-2px);
        }

        .button:active {
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <div class="generator-container">
        <h1>Tweak Generator</h1>
        <p>Create your unique Tweak image! Fill out the details below.</p>
        <form action="/generate" method="POST">
            <!-- Prompt Input -->
            <label for="prompt">What's your idea?</label>
            <input type="text" id="prompt" name="prompt" placeholder="e.g., A frog in Tweak style" required>

            <!-- LoRA Scale Slider -->
            <label for="lora_scale">How much LoRA magic?</label>
            <input type="range" id="lora_scale" name="lora_scale" min="0" max="3" step="0.1" value="1" class="slider">

            <!-- Number of Outputs -->
            <label for="num_outputs">How many images?</label>
            <input type="number" id="num_outputs" name="num_outputs" min="1" max="5" value="1">

            <!-- Aspect Ratio Dropdown -->
            <label for="aspect_ratio">Choose an aspect ratio</label>
            <select id="aspect_ratio" name="aspect_ratio">
                <option value="1:1">1:1 (Square)</option>
                <option value="16:9">16:9 (Landscape)</option>
                <option value="9:16">9:16 (Portrait)</option>
            </select>

            <!-- Guidance Scale Slider -->
            <label for="guidance_scale">How much guidance?</label>
            <input type="range" id="guidance_scale" name="guidance_scale" min="1" max="20" step="0.5" value="3.0" class="slider">

            <!-- Prompt Strength Slider -->
            <label for="prompt_strength">How strong is the prompt?</label>
            <input type="range" id="prompt_strength" name="prompt_strength" min="0" max="1" step="0.1" value="0.8" class="slider">

            <!-- Steps Slider -->
            <label for="num_inference_steps">How many steps?</label>
            <input type="range" id="num_inference_steps" name="num_inference_steps" min="1" max="50" value="28" class="slider">

            <!-- Submit Button -->
            <button type="submit" class="button">Generate My Tweak!</button>
        </form>
    </div>
</body>
</html>
